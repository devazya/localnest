/**
 * ComposerShell.jsx — REMOVED (Segment 7.1 clarification)
 *
 * Segment 7.1 reuses all existing creation flows as-is.
 * This file is intentionally empty — do not import it.
 *
 * Existing composers:
 *   CreateDiscussionSheet  → Start Discussion
 *   CreateUpdateModal      → Neighbourhood Update
 *   CreatePostModal        → Community Post
 *   UniversalPost          → All other types (PG, Ride, Event…)
 *
 * A shared ComposerShell will only be built in Segment 7.2 if
 * new post types genuinely require a net-new composer UI.
 */

// intentionally empty
export default null;
